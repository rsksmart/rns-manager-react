import React, { Component } from 'react';
import DomainStateContainer from '../containers/DomainStateContainer';

class DomainStateComponent extends Component {
  render () {
    return (
      <div>
        <DomainStateContainer />
      </div>
    );
  }
}

export default DomainStateComponent;
